
namespace booruReader.Helpers
{
    public enum PostRating
    {
        Safe,
        Questionable,
        Explicit
    };

    public enum ProviderAccessType
    {
        XML,
        JSON,
        Gelbooru,
        INVALID
    };
}
